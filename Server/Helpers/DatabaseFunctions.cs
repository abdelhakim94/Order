using System;

namespace Order.Server.Helpers
{
    public static class DatabaseFunctions
    {
        public static bool IsNear(int userId, decimal latitude, decimal longitude, decimal minDistance)
            => throw new NotImplementedException();

        public static string UserCategories(int userId)
            => throw new NotImplementedException();
    }
}
